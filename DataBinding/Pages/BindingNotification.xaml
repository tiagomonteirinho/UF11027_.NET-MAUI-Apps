<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="DataBinding.Pages.BindingNotification"
             Title="BindingNotification">
    <VerticalStackLayout Spacing="20">

        <Entry Text="{Binding Name}" FontSize="Large"/>
        <Entry Text="{Binding Price, StringFormat='Price: {0:F2}€'}" FontSize="Large"/>
        <Entry Text="{Binding Stock, StringFormat='Stock: {0}'}" FontSize="Large"/>

        <Button x:Name="update_button1" Clicked="update_button1_Clicked"
                Text="Update product" FontSize="Large"/>

    </VerticalStackLayout>
</ContentPage>